plugins {
    id "base"
    id 'org.itsallcode.openfasttrace'
}

requirementTracing {
  failBuild = false
  inputDirectories = files('custom-dir')
  reportFormat = 'html'
  detailsSectionDisplay = 'EXPAND'
}
