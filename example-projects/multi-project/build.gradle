plugins {
    id 'org.itsallcode.openfasttrace'
}

requirementTracing {
  inputDirectories = files('custom-dir-root')
  reportFile = file('build/custom-report.txt')
  pathConfig {
    rootPatternBalh { }
    'regex:rootPatternBlubb' { }
  }
}