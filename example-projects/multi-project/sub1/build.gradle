requirementTracing {
  inputDirectories = files('custom-dir1')
  pathConfig {
    'glob:src/*.test.*' {
      tagArtifactType = 'utest'
      coveredItemArtifactType = 'dsn'
    }
    'glob:src/*.impl.*' {
      coveredItemNamePrefix = 'prefix'
      tagArtifactType = 'impl'
      coveredItemArtifactType = 'dsn'
    }
  }
}
